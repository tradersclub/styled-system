import { system } from '@core/index';

export const shadow = system({
  boxShadow: {
    property: `boxShadow`,
    scale: `shadows`,
  },
  textShadow: {
    property: `textShadow`,
    scale: `shadows`,
  },
});

export default shadow;
